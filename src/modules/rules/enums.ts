enum RuleEntityName {
  Rule = 'rule',
  RuleVersion = 'rule version',
}

enum RuleVersionValidationStatus {
  Pending = 'pending',
  Valid = 'valid',
  Invalid = 'invalid',
}

export {RuleEntityName, RuleVersionValidationStatus};
